
## Phase 7: Strands Agent & AI Orchestration
ReStockAI uses the Strands Agents SDK to orchestrate the deterministic engines through a natural-language manager assistant layer. Tests can be run via pytest, and the agent can be tested locally via python cli.py.
